rger. Such files will be corrupted when
using Windows (unless smudging is disabled) with a Git for Windows
version less than 2.34.0 due to a limitation in Git. Default: true if
the version is less than 2.34.0, false otherwise.</p>
</div>
</li>
</ul>
</div>
</div>
<div class="sect2">
<h3 id="_upload_and_download_transfer_settings">Upload and download transfer settings</h3>
<div class="paragraph">
<p>These settings control how the upload and download of LFS content
occurs.</p>
</div>
<div class="ulist">
<ul>
<li>
<p><code>lfs.concurrenttransfers</code></p>
<div class="paragraph">
<p>The number of concurrent uploads/downloads. Default 8.</p>
</div>
</li>
<li>
<p><code>lfs.basictransfersonly</code></p>
<div class="paragraph">
<p>If set to true, only basic HTTP upload/download transfers will be used,
ignoring any more advanced transfers that the client/server may support.
This is primarily to work around bugs or incompatibilities.</p>
</div>
<div class="paragraph">
<p>The git-lfs client supports basic HTTP downloads, resumable HTTP
downloads (using <code>Range</code> headers), and resumable uploads via tus.io
protocol. Custom transfer methods can be added via <code>lfs.customtransfer</code>
(see next section). However setting this value to true limits the client
to simple HTTP.</p>
</div>
</li>
<li>
<p><code>lfs.tustransfers</code></p>
<div class="paragraph">
<p>If set to true, this enables resumable uploads of LFS objects through
the tus.io API. Once this feature is finalized, this setting will be
removed, and tus.io uploads will be available for all clients.</p>
</div>
</li>
<li>
<p><code>lfs.standalonetransferagent</code></p>
<div class="paragraph">
<p>Allows the specified custom transfer agent to be used directly for
transferring files, without asking the server how the transfers should
be made. The custom transfer agent has to be defined in a
<code>lfs.customtransfer.&lt;name&gt;</code> settings group.</p>
</div>
</li>
<li>
<p><code>lfs.customtransfer.&lt;name&gt;.path</code></p>
<div class="paragraph">
<p><code>lfs.customtransf